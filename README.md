# Report-9-1

---

# 📋 گزارش پروژه: سیستم تشخیص کلید با آردوینو (Key Detection System)

## ۱. مقدمه  
در این پروژه, یک **سنسور دیجیتال ساده** (احتمالاً یک Reed Switch یا دکمه مکانیکی) به آردوینو متصل شده است. وقتی کلید فلزی روی سنسور قرار داده می‌شود (یا دکمه فشرده می‌شود), سنسور وضعیت `LOW` (0) را ارسال می‌کند و آردوینو یک **LED سبز روشن** می‌کند. در غیر این صورت (کلید نباشد), سنسور وضعیت `HIGH` (1) را ارسال می‌کند و **LED سبز خاموش** می‌ماند.
.
”

## ۳. قطعات مورد استفاده  
- برد آردوینو Uno  
- یک سنسور دیجیتال (Reed Switch یا دکمه مکانیکی)  
- یک LED سبز (در تصویر روشن است)  
- یک مقاومت 220 اهم  
- برد برد و سیم‌های جامپر  
- یک کلید فلزی (برای فعال‌سازی سنسور — در صورت استفاده از Reed Switch)


## ۵. کد برنامه (اسکچ)

```cpp
int led = 2;  // پین LED

void setup() {
  pinMode(A0, INPUT);    // تنظیم A0 به عنوان ورودی دیجیتال
  pinMode(led, OUTPUT);  // تنظیم پین LED به عنوان خروجی
  Serial.begin(9600);    // شروع ارتباط سریال
}

void loop() {
  int IR;  // نام متغیر گمراه‌کننده — در واقع سنسور دیجیتال است، نه IR!
  IR = digitalRead(A0);  // خواندن وضعیت سنسور

  if (IR == 0) {  // اگر سنسور فعال شده (کلید حضور دارد)
    Serial.print("Object detected = ");
    Serial.println(IR);
    digitalWrite(led, HIGH);  // LED روشن
  } else {  // اگر سنسور غیرفعال است (کلید نیست)
    Serial.print("Not detected = ");
    Serial.println(IR);
    digitalWrite(led, LOW);   // LED خاموش
  }

  delay(300);  // تأخیر برای ثبات نمایش
}
```

## ۶. نحوه‌ی کارکرد  
1. وقتی کلید فلزی روی سنسور قرار داده می‌شود → سنسور وضعیت `LOW` (0) را ارسال می‌کند → LED سبز روشن می‌شود.  
2. وقتی کلید را برداشته می‌کنید → سنسور وضعیت `HIGH` (1) را ارسال می‌کند → LED سبز خاموش می‌شود.  
3. نتیجه در Serial Monitor نیز نمایش داده می‌شود.

## ۷. نتایج  
- سیستم به درستی کلید را تشخیص می‌دهد.  
- LED سبز به درستی روشن/خاموش می‌شود.  
- خروجی Serial Monitor نیز صحیح است.
 

---
